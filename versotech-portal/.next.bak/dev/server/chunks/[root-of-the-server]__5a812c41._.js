module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/punycode [external] (punycode, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("punycode", () => require("punycode"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[project]/versotech-portal/src/lib/supabase/server.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createClient",
    ()=>createClient,
    "createServiceClient",
    ()=>createServiceClient
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/@supabase/ssr/dist/module/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/@supabase/ssr/dist/module/createServerClient.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$supabase$2d$js$2f$dist$2f$module$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/@supabase/supabase-js/dist/module/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/next/headers.js [app-route] (ecmascript)");
;
;
;
const createClient = async ()=>{
    const cookieStore = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createServerClient"])(("TURBOPACK compile-time value", "https://ipguxdssecfexudnvtia.supabase.co"), ("TURBOPACK compile-time value", "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlwZ3V4ZHNzZWNmZXh1ZG52dGlhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTgzNjE4MzcsImV4cCI6MjA3MzkzNzgzN30.AGxM_YW9hfxuu7xDpi_4xhOoRhYM7iGTS1vJ0z2FByE"), {
        cookies: {
            getAll () {
                return cookieStore.getAll();
            },
            setAll (cookiesToSet) {
                try {
                    cookiesToSet.forEach(({ name, value, options })=>{
                        cookieStore.set(name, value, options);
                    });
                } catch (error) {
                // The `setAll` method was called from a Server Component.
                // This can be ignored if you have middleware refreshing
                // user sessions.
                }
            }
        }
    });
};
const createServiceClient = ()=>{
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$supabase$2d$js$2f$dist$2f$module$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["createClient"])(("TURBOPACK compile-time value", "https://ipguxdssecfexudnvtia.supabase.co"), process.env.SUPABASE_SERVICE_ROLE_KEY, {
        auth: {
            autoRefreshToken: false,
            persistSession: false
        }
    });
};
}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[project]/versotech-portal/src/lib/auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "AuthError",
    ()=>AuthError,
    "STAFF_ROLES",
    ()=>STAFF_ROLES,
    "checkCeoAccess",
    ()=>checkCeoAccess,
    "checkStaffAccess",
    ()=>checkStaffAccess,
    "getCurrentSession",
    ()=>getCurrentSession,
    "getCurrentUser",
    ()=>getCurrentUser,
    "getProfile",
    ()=>getProfile,
    "requireAuth",
    ()=>requireAuth,
    "requireInvestorAuth",
    ()=>requireInvestorAuth,
    "requireStaffAuth",
    ()=>requireStaffAuth
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/@supabase/ssr/dist/module/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/@supabase/ssr/dist/module/createServerClient.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/next/headers.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$dist$2f$api$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/next/dist/api/navigation.react-server.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$dist$2f$client$2f$components$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/next/dist/client/components/navigation.react-server.js [app-route] (ecmascript)");
;
;
;
const STAFF_ROLES = [
    'staff_admin',
    'staff_ops',
    'staff_rm',
    'ceo'
];
class AuthError extends Error {
    code;
    constructor(message, code){
        super(message), this.code = code;
        this.name = 'AuthError';
    }
}
// Server-side Supabase client
const createServerSupabaseClient = async ()=>{
    const cookieStore = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createServerClient"])(("TURBOPACK compile-time value", "https://ipguxdssecfexudnvtia.supabase.co"), ("TURBOPACK compile-time value", "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlwZ3V4ZHNzZWNmZXh1ZG52dGlhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTgzNjE4MzcsImV4cCI6MjA3MzkzNzgzN30.AGxM_YW9hfxuu7xDpi_4xhOoRhYM7iGTS1vJ0z2FByE"), {
        cookies: {
            getAll () {
                return cookieStore.getAll();
            },
            setAll (cookiesToSet) {
                try {
                    cookiesToSet.forEach(({ name, value, options })=>cookieStore.set(name, value, options));
                } catch  {
                // The `setAll` method was called from a Server Component.
                // This can be ignored if you have middleware refreshing
                // user sessions.
                }
            }
        }
    });
};
async function getCurrentUser() {
    try {
        const supabase = await createServerSupabaseClient();
        // Get current auth user
        const { data: { user }, error: userError } = await supabase.auth.getUser();
        if (userError || !user) {
            return null;
        }
        // Get profile data from database
        const { data: profile, error: profileError } = await supabase.from('profiles').select('*').eq('id', user.id).single();
        if (profileError || !profile) {
            console.error('[auth] Profile not found for user:', user.id, profileError);
            return null;
        }
        // Fetch permissions for staff users
        let permissions = [];
        const isStaffRole = STAFF_ROLES.includes(profile.role);
        if (isStaffRole) {
            const { data: permissionsData } = await supabase.from('staff_permissions').select('permission').eq('user_id', user.id);
            permissions = permissionsData?.map((p)=>p.permission) || [];
        }
        return {
            id: profile.id,
            email: profile.email,
            displayName: profile.display_name || profile.email?.split('@')[0] || 'User',
            avatar: profile.avatar_url,
            role: profile.role,
            title: profile.title,
            created_at: profile.created_at,
            permissions
        };
    } catch (error) {
        console.error('Error getting current user:', error);
        return null;
    }
}
async function getCurrentSession() {
    try {
        const supabase = await createServerSupabaseClient();
        const { data: { session }, error } = await supabase.auth.getSession();
        if (error) {
            console.error('Error getting session:', error);
            return null;
        }
        return session;
    } catch (error) {
        console.error('Error getting session:', error);
        return null;
    }
}
async function getProfile() {
    const user = await getCurrentUser();
    if (user) {
        console.log('[auth] Profile retrieved:', user.email, user.role);
    } else {
        console.log('[auth] No profile found');
    }
    return user;
}
async function requireAuth(allowedRoles) {
    const user = await getCurrentUser();
    if (!user) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$dist$2f$client$2f$components$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["redirect"])('/versotech_main/login');
    }
    if (allowedRoles && !allowedRoles.includes(user.role)) {
        throw new Error('Insufficient permissions');
    }
    return user;
}
async function requireInvestorAuth() {
    const user = await getCurrentUser();
    if (!user) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$dist$2f$client$2f$components$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["redirect"])('/versotech_main/login');
    }
    if (user.role !== 'investor') {
        throw new Error('Investor access required');
    }
    return user;
}
async function requireStaffAuth() {
    const user = await getCurrentUser();
    if (!user) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$dist$2f$client$2f$components$2f$navigation$2e$react$2d$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["redirect"])('/versotech_main/login');
    }
    if (!STAFF_ROLES.includes(user.role)) {
        throw new Error('Staff access required');
    }
    return user;
}
async function checkStaffAccess(userId) {
    const supabase = await createServerSupabaseClient();
    // Check profile role first (handles synthetic staff personas)
    const { data: profile, error: profileError } = await supabase.from('profiles').select('role').eq('id', userId).single();
    if (!profileError && profile && STAFF_ROLES.includes(profile.role)) {
        return true;
    }
    // Fallback: check database personas (for edge cases)
    const { data: personas } = await supabase.rpc('get_user_personas', {
        p_user_id: userId
    });
    return personas?.some((p)=>p.persona_type === 'staff' || p.persona_type === 'ceo') || false;
}
async function checkCeoAccess(userId) {
    const supabase = await createServerSupabaseClient();
    const { data: profile } = await supabase.from('profiles').select('role').eq('id', userId).single();
    return profile?.role === 'ceo' || profile?.role === 'staff_admin';
}
;
}),
"[project]/versotech-portal/src/lib/fees/subscription-fee-calculator.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Subscription Fee Calculator
 * Calculates fee events from subscription data
 *
 * Source of Truth: Subscription table (amounts)
 * Frequency Source: Fee plan (if linked)
 */ __turbopack_context__.s([
    "calculateSubscriptionFeeEvents",
    ()=>calculateSubscriptionFeeEvents,
    "createFeeEvents",
    ()=>createFeeEvents
]);
async function calculateSubscriptionFeeEvents(supabase, subscriptionId) {
    try {
        // Fetch subscription with fee plan and vehicle
        const { data: subscription, error: subError } = await supabase.from('subscriptions').select(`
        *,
        vehicle:vehicles (
          id,
          name
        ),
        fee_plan:fee_plans (
          id,
          components:fee_components (
            kind,
            frequency,
            payment_schedule
          )
        )
      `).eq('id', subscriptionId).single();
        if (subError || !subscription) {
            return {
                success: false,
                error: subError?.message || 'Subscription not found'
            };
        }
        const feeEvents = [];
        const fees = subscription;
        const feePlan = subscription.fee_plan;
        // Helper to get frequency/payment_schedule from fee plan
        const getFrequencyInfo = (kind)=>{
            if (!feePlan || !feePlan.components) {
                // No fee plan: default to one_time/upfront
                return {
                    frequency: 'one_time',
                    payment_schedule: 'upfront'
                };
            }
            const component = feePlan.components.find((c)=>c.kind === kind);
            return {
                frequency: component?.frequency || 'one_time',
                payment_schedule: component?.payment_schedule || 'upfront'
            };
        };
        // 0. COMMITMENT AMOUNT (the investment itself)
        // This is the primary payment - the investor's actual commitment to the vehicle
        const commitmentAmount = subscription.commitment || 0;
        if (commitmentAmount > 0) {
            feeEvents.push({
                fee_type: 'flat',
                base_amount: commitmentAmount,
                computed_amount: commitmentAmount,
                rate_bps: null,
                frequency: 'one_time',
                payment_schedule: 'upfront',
                description: `Investment commitment - ${subscription.vehicle?.name || 'vehicle'}`
            });
        }
        // 1. Subscription Fee
        // Note: Check for != null to properly handle explicit 0 values (0 means "no fee")
        if (fees.subscription_fee_percent != null || fees.subscription_fee_amount != null) {
            const baseAmount = subscription.commitment || 0;
            const { frequency, payment_schedule } = getFrequencyInfo('subscription');
            // Use explicit amount if provided (even if 0), otherwise calculate from percent
            const computedAmount = fees.subscription_fee_amount != null ? fees.subscription_fee_amount : baseAmount * (fees.subscription_fee_percent || 0) / 100;
            feeEvents.push({
                fee_type: 'subscription',
                base_amount: baseAmount,
                computed_amount: computedAmount,
                rate_bps: fees.subscription_fee_percent ? Math.round(fees.subscription_fee_percent * 100) : null,
                frequency,
                payment_schedule,
                description: `Subscription fee for ${subscription.commitment || 0} commitment`
            });
        }
        // 2. Management Fee
        // Note: Check for != null to properly handle explicit 0 values (0 means "no fee")
        if (fees.management_fee_percent != null || fees.management_fee_amount != null) {
            const baseAmount = subscription.commitment || 0;
            const { frequency, payment_schedule } = getFrequencyInfo('management');
            // Use subscription's frequency if specified, otherwise use fee plan
            const finalFrequency = fees.management_fee_frequency || frequency;
            // Use explicit amount if provided (even if 0), otherwise calculate from percent
            const computedAmount = fees.management_fee_amount != null ? fees.management_fee_amount : baseAmount * (fees.management_fee_percent || 0) / 100;
            feeEvents.push({
                fee_type: 'management',
                base_amount: baseAmount,
                computed_amount: computedAmount,
                rate_bps: fees.management_fee_percent ? Math.round(fees.management_fee_percent * 100) : null,
                frequency: finalFrequency,
                payment_schedule,
                description: `Management fee for ${subscription.commitment || 0} commitment`
            });
        }
        // 3. BD Fee (Broker-Dealer)
        // Note: Check for != null to properly handle explicit 0 values (0 means "no fee")
        if (fees.bd_fee_percent != null || fees.bd_fee_amount != null) {
            const baseAmount = subscription.commitment || 0;
            const { frequency, payment_schedule } = getFrequencyInfo('bd_fee');
            // Use explicit amount if provided (even if 0), otherwise calculate from percent
            const computedAmount = fees.bd_fee_amount != null ? fees.bd_fee_amount : baseAmount * (fees.bd_fee_percent || 0) / 100;
            feeEvents.push({
                fee_type: 'bd_fee',
                base_amount: baseAmount,
                computed_amount: computedAmount,
                rate_bps: fees.bd_fee_percent ? Math.round(fees.bd_fee_percent * 100) : null,
                frequency,
                payment_schedule,
                description: `Broker-dealer fee for ${subscription.commitment || 0} commitment`
            });
        }
        // 4. FINRA Fee
        if (fees.finra_fee_amount) {
            const { frequency, payment_schedule } = getFrequencyInfo('finra_fee');
            feeEvents.push({
                fee_type: 'finra_fee',
                base_amount: fees.finra_fee_amount,
                computed_amount: fees.finra_fee_amount,
                rate_bps: null,
                frequency,
                payment_schedule,
                description: 'FINRA regulatory fee'
            });
        }
        // 5. Spread Fee
        if (fees.spread_fee_amount) {
            const { frequency, payment_schedule } = getFrequencyInfo('spread_markup');
            feeEvents.push({
                fee_type: 'spread_markup',
                base_amount: fees.spread_fee_amount,
                computed_amount: fees.spread_fee_amount,
                rate_bps: null,
                frequency,
                payment_schedule,
                description: 'Spread markup fee'
            });
        }
        // 6. Performance Fee (Tier 1 and Tier 2)
        if (fees.performance_fee_tier1_percent || fees.performance_fee_tier2_percent) {
            const { frequency, payment_schedule } = getFrequencyInfo('performance');
            // Calculate actual performance fee if NAV exists
            const commitment = subscription.commitment || 0;
            const nav = subscription.current_nav || 0;
            let performanceFee = 0;
            const tier1Threshold = fees.performance_fee_tier1_threshold || 0;
            const tier2Threshold = fees.performance_fee_tier2_threshold || 0;
            const tier1Percent = fees.performance_fee_tier1_percent || 0;
            const tier2Percent = fees.performance_fee_tier2_percent || 0;
            // Calculate returns-based performance fee (same logic as overview page)
            if (nav > 0 && commitment > 0) {
                const returns = nav - commitment;
                if (returns > 0) {
                    if (tier2Percent > 0 && returns > tier2Threshold) {
                        performanceFee = returns * (tier2Percent / 100);
                    } else if (tier1Percent > 0 && returns > tier1Threshold) {
                        performanceFee = returns * (tier1Percent / 100);
                    }
                }
            }
            // Create description based on which tier applies
            let description = 'Performance fee';
            if (tier2Percent > 0 && tier2Threshold > 0) {
                description = `Performance fee (Tier 2: ${tier2Percent}% above ${tier2Threshold} returns)`;
            } else if (tier1Percent > 0) {
                description = `Performance fee (Tier 1: ${tier1Percent}% above ${tier1Threshold || 0} returns)`;
            }
            feeEvents.push({
                fee_type: 'performance',
                base_amount: nav > 0 ? nav - commitment : 0,
                computed_amount: performanceFee,
                rate_bps: tier2Percent > 0 ? Math.round(tier2Percent * 100) : Math.round(tier1Percent * 100),
                frequency,
                payment_schedule,
                description
            });
        }
        return {
            success: true,
            feeEvents
        };
    } catch (error) {
        console.error('Error calculating subscription fee events:', error);
        return {
            success: false,
            error: error instanceof Error ? error.message : 'Unknown error'
        };
    }
}
async function createFeeEvents(supabase, subscriptionId, investorId, dealId, feePlanId, feeEvents, options) {
    try {
        // Deduplication check: Skip if fee events already exist for this subscription
        // This prevents double-charging if createFeeEvents is called multiple times
        if (!options?.skipDeduplication) {
            const { data: existingEvents, error: checkError } = await supabase.from('fee_events').select('id').eq('allocation_id', subscriptionId).limit(1);
            if (checkError) {
                console.warn('[FEE EVENTS] Error checking existing fee events:', checkError.message);
            // Continue anyway - better to risk duplicates than fail silently
            } else if (existingEvents && existingEvents.length > 0) {
                console.log(`[FEE EVENTS] Fee events already exist for subscription ${subscriptionId}, skipping creation`);
                return {
                    success: true,
                    feeEventIds: [],
                    skipped: true
                };
            }
        }
        // Fetch subscription to get currency
        const { data: subscription } = await supabase.from('subscriptions').select('currency').eq('id', subscriptionId).single();
        const currency = subscription?.currency || 'USD';
        // Determine payee_arranger_id - first check fee_plan, then deal
        let payeeArrangerId = null;
        // Check if fee_plan was created by an arranger
        if (feePlanId) {
            const { data: feePlan } = await supabase.from('fee_plans').select('created_by_arranger_id').eq('id', feePlanId).single();
            if (feePlan?.created_by_arranger_id) {
                payeeArrangerId = feePlan.created_by_arranger_id;
            }
        }
        // If no arranger from fee_plan, check deal
        if (!payeeArrangerId && dealId) {
            const { data: deal } = await supabase.from('deals').select('arranger_entity_id').eq('id', dealId).single();
            if (deal?.arranger_entity_id) {
                payeeArrangerId = deal.arranger_entity_id;
            }
        }
        // If we have a fee plan, fetch its components to link properly
        const componentMap = {};
        if (feePlanId) {
            const { data: components } = await supabase.from('fee_components').select('id, kind').eq('fee_plan_id', feePlanId);
            if (components) {
                // Map fee_type to component_id
                components.forEach((comp)=>{
                    componentMap[comp.kind] = comp.id;
                });
            }
        }
        const inserts = feeEvents.map((fe)=>{
            // Match fee_type to fee_component_id
            // fee_type 'flat' (commitment) doesn't get a component_id
            const feeComponentId = fe.fee_type === 'flat' ? null : componentMap[fe.fee_type] || null;
            // Warn if fee type expected a component but none found
            if (fe.fee_type !== 'flat' && !feeComponentId && feePlanId) {
                console.warn(`⚠️ [FEE EVENTS] No component found for fee_type '${fe.fee_type}' in fee plan ${feePlanId}. ` + `Fee event will be created without component link.`);
            }
            return {
                investor_id: investorId,
                deal_id: dealId,
                allocation_id: subscriptionId,
                fee_component_id: feeComponentId,
                fee_type: fe.fee_type,
                event_date: new Date().toISOString(),
                base_amount: fe.base_amount,
                computed_amount: fe.computed_amount,
                rate_bps: fe.rate_bps,
                currency: currency,
                status: 'accrued',
                notes: fe.description,
                payee_arranger_id: payeeArrangerId
            };
        });
        const { data, error } = await supabase.from('fee_events').insert(inserts).select('id');
        if (error) {
            return {
                success: false,
                error: error.message
            };
        }
        return {
            success: true,
            feeEventIds: data?.map((d)=>d.id) || []
        };
    } catch (error) {
        console.error('Error creating fee events:', error);
        return {
            success: false,
            error: error instanceof Error ? error.message : 'Unknown error'
        };
    }
}
}),
"[project]/versotech-portal/src/app/api/subscriptions/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET,
    "PATCH",
    ()=>PATCH,
    "POST",
    ()=>POST,
    "dynamic",
    ()=>dynamic
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/src/lib/supabase/server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/src/lib/auth.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$fees$2f$subscription$2d$fee$2d$calculator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/versotech-portal/src/lib/fees/subscription-fee-calculator.ts [app-route] (ecmascript)");
;
;
;
;
const dynamic = 'force-dynamic';
async function GET(request) {
    try {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["requireStaffAuth"])();
        const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
        const { searchParams } = new URL(request.url);
        const vehicleId = searchParams.get('vehicle');
        const investorId = searchParams.get('investor_id');
        const status = searchParams.get('status');
        const search = searchParams.get('q');
        // Default to fetching ALL subscriptions (no pagination by default)
        const limit = searchParams.get('limit') ? parseInt(searchParams.get('limit')) : null;
        const page = limit ? parseInt(searchParams.get('page') || '1') : null;
        const offset = limit && page ? (page - 1) * limit : null;
        // Build query
        let query = supabase.from('subscriptions').select(`
          *,
          investor:investors (
            id,
            legal_name,
            type,
            country,
            kyc_status
          ),
          vehicle:vehicles (
            id,
            name,
            type,
            currency,
            status,
            entity_code,
            investment_name
          ),
          introducer:introducers (
            id,
            legal_name
          ),
          deal:deals (
            id,
            name
          )
        `, {
            count: 'exact'
        }).order('subscription_number', {
            ascending: false
        });
        // Apply filters
        if (vehicleId) {
            query = query.eq('vehicle_id', vehicleId);
        }
        if (investorId) {
            query = query.eq('investor_id', investorId);
        }
        if (status) {
            query = query.eq('status', status);
        }
        if (search) {
            // Search by subscription number or investor name
            const searchNum = parseInt(search);
            if (!isNaN(searchNum)) {
                query = query.eq('subscription_number', searchNum);
            } else {
                // This requires a join query - we'll handle it differently
                query = query.or(`investor.legal_name.ilike.%${search}%`);
            }
        }
        // Apply pagination only if limit is specified
        if (offset !== null && limit !== null) {
            query = query.range(offset, offset + limit - 1);
        }
        const { data: subscriptions, error, count } = await query;
        if (error) {
            console.error('[Subscriptions API] Error:', error);
            return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: 'Failed to fetch subscriptions',
                details: error.message
            }, {
                status: 500
            });
        }
        // Calculate summary statistics
        const allSubscriptionsQuery = await supabase.from('subscriptions').select('status, commitment, currency, funding_due_at');
        const allSubs = allSubscriptionsQuery.data || [];
        const summary = {
            total: count || 0,
            by_status: allSubs.reduce((acc, sub)=>{
                acc[sub.status] = (acc[sub.status] || 0) + 1;
                return acc;
            }, {}),
            by_currency: allSubs.reduce((acc, sub)=>{
                acc[sub.currency || 'USD'] = (acc[sub.currency || 'USD'] || 0) + (sub.commitment || 0);
                return acc;
            }, {}),
            total_commitment: allSubs.reduce((sum, sub)=>sum + (sub.commitment || 0), 0),
            overdue_count: allSubs.filter((sub)=>sub.status === 'committed' && sub.funding_due_at && new Date(sub.funding_due_at) < new Date()).length,
            overdue_amount: allSubs.filter((sub)=>sub.status === 'committed' && sub.funding_due_at && new Date(sub.funding_due_at) < new Date()).reduce((sum, sub)=>sum + (sub.commitment || 0), 0)
        };
        return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            data: subscriptions || [],
            subscriptions: subscriptions || [],
            summary,
            pagination: limit ? {
                page: page || 1,
                limit,
                total: count || 0,
                totalPages: Math.ceil((count || 0) / limit)
            } : null
        });
    } catch (error) {
        console.error('[Subscriptions API] Exception:', error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: 'Internal server error'
        }, {
            status: 500
        });
    }
}
async function PATCH(request) {
    try {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["requireStaffAuth"])();
        const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
        const serviceSupabase = (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createServiceClient"])();
        const body = await request.json();
        const { subscription_ids, updates } = body;
        if (!subscription_ids || !Array.isArray(subscription_ids) || subscription_ids.length === 0) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: 'subscription_ids array is required'
            }, {
                status: 400
            });
        }
        if (!updates || typeof updates !== 'object') {
            return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: 'updates object is required'
            }, {
                status: 400
            });
        }
        // Validate allowed fields
        const allowedFields = [
            'status',
            'effective_date',
            'funding_due_at',
            'acknowledgement_notes'
        ];
        const updateFields = Object.keys(updates);
        const invalidFields = updateFields.filter((field)=>!allowedFields.includes(field));
        if (invalidFields.length > 0) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: `Invalid fields: ${invalidFields.join(', ')}`
            }, {
                status: 400
            });
        }
        // If status is being changed to 'committed', fetch subscriptions to check old status
        let subsToAutoCalculateFees = [];
        if (updates.status === 'committed') {
            const { data: existingSubs } = await serviceSupabase.from('subscriptions').select('id, status').in('id', subscription_ids);
            // Only auto-calculate fees for subscriptions that are NOT already committed
            subsToAutoCalculateFees = (existingSubs || []).filter((sub)=>sub.status !== 'committed').map((sub)=>sub.id);
        }
        const { error } = await supabase.from('subscriptions').update({
            ...updates,
            updated_at: new Date().toISOString()
        }).in('id', subscription_ids);
        if (error) {
            console.error('[Subscriptions API] Bulk update error:', error);
            return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: 'Failed to update subscriptions',
                details: error.message
            }, {
                status: 500
            });
        }
        // AUTO-CALCULATE FEE EVENTS for subscriptions that became 'committed'
        let feeEventsCreatedCount = 0;
        if (subsToAutoCalculateFees.length > 0) {
            console.log(`[Subscriptions API] Auto-calculating fee events for ${subsToAutoCalculateFees.length} subscription(s)`);
            for (const subId of subsToAutoCalculateFees){
                // Check if fee events already exist
                const { data: existingFeeEvents } = await serviceSupabase.from('fee_events').select('id').eq('allocation_id', subId).limit(1);
                if (!existingFeeEvents || existingFeeEvents.length === 0) {
                    // Get subscription details
                    const { data: sub } = await serviceSupabase.from('subscriptions').select('investor_id, vehicle_id, fee_plan_id').eq('id', subId).single();
                    if (sub) {
                        // Calculate fee events
                        const calculationResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$fees$2f$subscription$2d$fee$2d$calculator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["calculateSubscriptionFeeEvents"])(serviceSupabase, subId);
                        if (calculationResult.success && calculationResult.feeEvents && calculationResult.feeEvents.length > 0) {
                            // Create fee events (subscriptions don't have deal_id)
                            const creationResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$fees$2f$subscription$2d$fee$2d$calculator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createFeeEvents"])(serviceSupabase, subId, sub.investor_id, null, sub.fee_plan_id || null, calculationResult.feeEvents);
                            if (creationResult.success) {
                                feeEventsCreatedCount++;
                            }
                        }
                    }
                }
            }
        }
        return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            success: true,
            message: `Successfully updated ${subscription_ids.length} subscription(s)`,
            updated_count: subscription_ids.length,
            fee_events_created_for: feeEventsCreatedCount
        });
    } catch (error) {
        console.error('[Subscriptions API] PATCH exception:', error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: 'Internal server error'
        }, {
            status: 500
        });
    }
}
async function POST(request) {
    try {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["requireStaffAuth"])();
        const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
        const body = await request.json();
        const { subscription_ids, format = 'csv' } = body;
        let query = supabase.from('subscriptions').select(`
          *,
          investor:investors (
            legal_name,
            type,
            country
          ),
          vehicle:vehicles (
            name,
            type,
            currency
          )
        `).order('subscription_number', {
            ascending: true
        });
        if (subscription_ids && Array.isArray(subscription_ids) && subscription_ids.length > 0) {
            query = query.in('id', subscription_ids);
        }
        const { data, error } = await query;
        if (error) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: 'Failed to fetch subscriptions for export'
            }, {
                status: 500
            });
        }
        // For now, return JSON - CSV formatting can be added later
        return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            data,
            count: data?.length || 0,
            format
        });
    } catch (error) {
        console.error('[Subscriptions API] Export exception:', error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$versotech$2d$portal$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: 'Internal server error'
        }, {
            status: 500
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__5a812c41._.js.map