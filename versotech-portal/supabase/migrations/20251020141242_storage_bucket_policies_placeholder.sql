-- This migration was already applied manually via execute_sql
-- Just registering it in the schema_migrations table

-- Policies already exist, so this is a no-op registration
SELECT 'Storage bucket policies already applied' as status;
